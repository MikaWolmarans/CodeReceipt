# Implementation Plans

Generated by the improve skill on 2026-06-12 against commit `36c4043`. Execute
in the order below unless dependencies say otherwise. Each executor: read the
plan fully before starting, honor its STOP conditions, and update your row when
done.

Repo verification gate (established by plan 001): `python3 -m pytest -q` from
the repo root. Before plan 001 lands there is **no** verification command —
only `python3 -m compileall -q app`.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Establish pytest verification baseline | P1 | M | — | DONE |
| 002 | Paid 30-day download survives session TTL | P1 | S | 001 | TODO |
| 003 | Fix free-scan email CTA (402 dead-end) | P1 | S | 001 (002 first: same file) | TODO |
| 004 | Idempotent, fast Stripe webhook fulfillment | P1 | M | 001, 003 | TODO |
| 005 | Implement the real paid analysis pipeline | P2 | L | 001, 004 | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- **001 first, always.** Plans 002–005 verify via the pytest suite it creates; 004 and 005 rewrite tests it writes (the webhook 500-path test in 001 deliberately documents the bug that 004 fixes).
- **002 before 003**: both edit `app/services/email.py` (002: paid email; 003: free email). Logically independent, but sequential execution avoids merge conflicts.
- **003 before 004**: both edit the frontend payment-return/view-state code; 004's poll change assumes 003's `paymentStatus === 'success'` guard is in place.
- **004 before 005 (hard)**: 005 inserts its re-analysis branch into the `_fulfill_order` background task that 004 creates. 005 has an explicit STOP condition if `_fulfill_order` is absent.
- 002 and 004 both touch `app/routers/export.py` (002: pdf_store fallback; 004: relies on its regeneration path; 005: adds a status guard). Run the export tests after each.

## Audit summary (2026-06-12, standard depth)

Audited directly (no subagents — repo is ~33 Python files + 1 frontend file):
all routers, services, ingestion, config, deployment files, frontend payment
flow. **Not audited**: `app/services/pdf/builder.py` internals (668 lines,
skimmed only), `app/services/pdf/template.html`, frontend static/marketing
markup, Dockerfile base-image CVEs.

Findings planned: #1 (paid link dies after TTL → 002), #2 (webhook idempotency
→ 004), #3 (paid pipeline not implemented → 005), #4 (free email CTA → 003),
#6 (no tests → 001).

## Findings deferred (worth doing, no plan yet)

- **#5 Ingestion rework**: GitHub blob-by-blob N+1 fetching (one API call per
  file, burns rate limit); `commit_sha` never captured so the analysis cache
  key is URL-only (stale results after a push, within session TTL); GitLab and
  Bitbucket hardcode the `main` branch and fail on `master` repos. One fix:
  switch to zipball/codeload endpoints. Evidence: `app/services/ingestion/github.py:69-88, 96, 107`,
  `app/routers/ingest.py:117`.
- **#7 Per-IP rate limiting + bounded upload reads**: only global daily caps
  exist (`app/middleware/rate_limit.py`); `await zip_file.read()` at
  `app/routers/ingest.py:120` reads the whole body before any size check.
- **#8 `/stats` counter decays**: counts `pdf_store` docs which TTL-expire
  after 30 days (`app/services/session.py:124-125`). Use a monotonic counter.
- **#9 Stale README + no CLAUDE.md**: README documents WeasyPrint/Jinja2/Gemma;
  reality is Playwright/chevron/gpt-4o-mini/Stripe; new endpoints and env vars
  undocumented.
- **#10 Frontend ships React dev builds + Babel-standalone** (`frontend/index.html:28-30`).

## Direction options (maintainer's call)

- Customer "my manuals" email lookup — `get_customer_sessions` exists unused
  (`app/services/session.py:118-120`).
- Watermarked one-page PDF preview in the teaser (conversion experiment).
- Workshop tier / change-receipt re-scans — `tier: 'workshop'` already stubbed
  in the model; pointless until plan 005 lands.

## Findings considered and rejected

- Cache-hit requests consume a global daily-cap slot (`enforce_daily_limit`
  runs before the cache check in `ingest.py`): real but trivial impact at
  current caps; revisit if caps tighten.
- Free-tier truncation takes the first 15 files in tree order, not "most
  important": quality gap, noted in plan 005's maintenance notes; not worth a
  standalone plan until the paid pipeline exists.
- `Cache-Control: no-store` on all static assets (security middleware): minor
  perf cost, deliberate-looking; bundle into #10 if that work happens.
- Dead legacy provider config fields (ollama/gemini/groq in `app/config.py`):
  harmless noise; delete opportunistically during any config change.
